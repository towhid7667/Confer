<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>com.github.towhid7667.confer</id>
    <name>Confer</name>
    <vendor>towhid7667</vendor>
    <description><![CDATA[
        <p><b>Confer</b> brings Claude Code into JetBrains IDEs as a native chat panel, wrapping the <code>claude</code> CLI as a subprocess instead of shelling out to a terminal.</p>
        <ul>
            <li><b>Streaming chat</b> with markdown rendering, collapsible thinking blocks, and inline tool-use/diff cards</li>
            <li><b>Native diff review</b> for every edit, with a real pre-execution approval flow for IDE-initiated edits via a local MCP server</li>
            <li><b>Multi-session tabs</b> — open in the sidebar, an editor tab, or a detached window, each with its own independent Claude process and history</li>
            <li><b>Context injection</b> from your editor selection (<code>Shift+Alt+C</code>), fuzzy <code>@</code>-mention file search, and automatic active-selection context</li>
            <li><b>Session history</b> with search, rename, and delete, backed by the CLI's own session transcripts</li>
            <li><b>Plan mode</b> that opens as a full Markdown document you can edit and send back as feedback before approving</li>
            <li>Permission modes, model switching, and a live cost/context-window meter</li>
        </ul>
        <p>Requires the <a href="https://code.claude.com/docs/en/overview">Claude Code CLI</a> to be installed and authenticated.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <actions>
        <action
            id="Confer.InjectContext"
            class="com.github.towhid7667.confer.actions.InjectContextAction"
            text="Inject into Confer Chat"
            description="Injects the current selection (or file path) into the Confer chat input as a @file#L reference"
        >
            <keyboard-shortcut keymap="$default" first-keystroke="shift alt C"/>
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>
        </action>
        <action
            id="Confer.OpenChat"
            class="com.github.towhid7667.confer.actions.OpenConferAction"
            text="Open Confer Chat"
            description="Opens the Confer chat panel"
            icon="/icons/confer.svg"
        >
            <!-- Best-effort placement: the floating icon group in the top-right of the editor.
                 Unverified against this exact platform version — check visually via runIde. -->
            <add-to-group group-id="EditorContextBarMenu" anchor="last"/>
        </action>
    </actions>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow
            factoryClass="com.github.towhid7667.confer.toolWindow.ConferToolWindowFactory"
            id="Confer"
            anchor="right"
            secondary="false"
            canCloseContents="false"
            icon="/icons/confer.svg"
        />
        <applicationService
            serviceImplementation="com.github.towhid7667.confer.settings.ClaudeSettings"
        />
        <applicationConfigurable
            id="com.github.towhid7667.confer.settings.ClaudeConfigurable"
            instance="com.github.towhid7667.confer.settings.ClaudeConfigurable"
            displayName="Confer"
        />
        <fileEditorProvider implementation="com.github.towhid7667.confer.toolWindow.ConferFileEditorProvider"/>
        <postStartupActivity implementation="com.github.towhid7667.confer.mcp.ConferIdeStartupActivity"/>
        <statusBarWidgetFactory id="ConferStatusBarWidget" implementation="com.github.towhid7667.confer.statusbar.ConferStatusBarWidgetFactory"/>
        <notificationGroup id="Confer" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
